{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "synth": "lerna run synth"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
